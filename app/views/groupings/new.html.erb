<%= form_for @grouping do |f| %>

  <% if @grouping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grouping.errors.count, "error") %> prohibited this quiz from being saved:</h2>

      <ul>
      <% @grouping.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="form-group">
		<%= f.label :name %>
		<%= f.text_field :name %>
    </div>
    
    <div class="form-group">
    	<%= f.label :tags %>
    	<%= f.text_field :tag_list %>
    </div>

  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>